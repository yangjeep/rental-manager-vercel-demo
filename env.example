# Google Sheets → Apps Script 部署 URL
NEXT_PUBLIC_LISTINGS_URL=https://script.google.com/macros/s/XXXX/exec

# 页面重新生成（ISR）时间，单位：秒
REVALIDATE_SECONDS=60

# Demo 环境用户名/密码（Basic Auth）
# If DEMO_USER is set, Basic Auth is enabled (for dev/staging/preview)
# Leave DEMO_USER unset in production for public access
DEMO_USER=demo
DEMO_PASS=demo123

# 手动触发刷新接口 /api/revalidate?secret=changeme
REVALIDATE_SECRET=changeme

# Demo 阶段禁止搜索引擎收录
DEMO_NOINDEX=true

# HubSpot 表单嵌入（如果暂时不用，可留空）
NEXT_PUBLIC_HUBSPOT_PORTAL_ID=
NEXT_PUBLIC_HUBSPOT_FORM_ID=

# Google Maps JavaScript API Key (optional, if using JavaScript API)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Google My Maps Map ID (for iframe embedding)
# Get this from your Google My Maps embed URL: https://www.google.com/maps/d/embed?mid=YOUR_MAP_ID
NEXT_PUBLIC_GOOGLE_MAPS_ID=1FjdzczdBlhZ5gV1nAh7IBLpomib8Abg

# Airtable Configuration (required)
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=Properties
# Note: The contact form submits to a "tenant-leads" table in the same Airtable base.
# Required fields in tenant-leads table: Title (from Property), Name, Email, Phone Number
# Optional fields: Ideal Move-in Date, Number of Occupants, Employment Status, Message
# Note: "Title (from Property)" should be a Linked Record field to the Properties table

# Google Drive Images Endpoint (optional, fallback if R2 images not found)
# Deploy scripts/apps_script.gs as a Google Apps Script web app and paste the URL here
# The endpoint should accept ?folder=<folderId> and return JSON array of image URLs
DRIVE_LIST_ENDPOINT=

# Cloudflare R2 Image Storage (recommended)
# Public URL of your R2 bucket (from R2 Settings > Public Access)
# Example: https://pub-8ad53ad0fd6f414fad3c0fdb189ec060.r2.dev
R2_PUBLIC_URL=https://pub-8ad53ad0fd6f414fad3c0fdb189ec060.r2.dev

# R2 API Credentials (REQUIRED for listing images)
# Get these from Cloudflare Dashboard → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=rental-manager-images

